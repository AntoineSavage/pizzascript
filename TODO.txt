replace AstExpr with quoted PzVal (eval becomes unquote, quote becomes id, unquote becomes double-unquote, etc)
add back enough pos stuff to have stack traces and debug info

not/or/and must return value with invoc position
enforce at compile time not empty: ident, func body
test/rename reduce (aka Acc stuff)

implement built-in functions (see Symbs)

error handling context field handling
I/O context field handling

base:
    - defun, cond, list, dict